# Multithreaded Custom Load Balancer

This load balancer is for the mock SaaS business website Maltoki. It utilizes the Crow C++ web framework as a frontend layer for taking requests and distributes them across multiple worker server encrypted socket connections.
